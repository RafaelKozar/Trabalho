<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <!-- Bootstrap core CSS -->
    <link href="http://getbootstrap.com/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-1.9.1.min.js"></script>
    <script src="http://getbootstrap.com/dist/js/bootstrap.min.js"></script>
    <!-- IE10 viewport hack for Surface/desktop Windows 8 bug -->
    <script src="http://getbootstrap.com/assets/js/ie10-viewport-bug-workaround.js"></script>
    <link rel="stylesheet" href="../stylesheets/estilo.css" />
    <script src="https://cdn.socket.io/socket.io-1.2.0.js"></script>
    <script>
       /* var socket = io.connect('http://localhost:3000');
        var url = document.location.href;
        var valor = url.split('/');
        var idRobo = valor[valor.length - 1];
        var mIdPaciente;
        var global = function () {
            var id = mIdPaciente;
        }

        socket.on('vai', function () {
            console.log("ehehe");
        });

        var callerIdEntry = document.querySelector('#caller-id');

        socket.on(idRobo, function (dado) {            
            mIdPaciente = dado.idPaciente;
            /*var elementpac = document.getElementById("recipient-id");
            var elementrob = document.getElementById("caller-id");

            elementpac.innerHTML = mIdPaciente;
            elementrob.innerHTML = idRobo;*/
            /*$("recipient-id").val() = mIdPaciente;

       //     $("#d").change();


            $("caller-id").val() = idRobo;
            dial()
        }); */
    </script>   

</head>
<script>
      

    </script>

<body>
    <input type="text"  id="recipient-id" />
    <input type="text"  id="caller-id">
    
    <div class="row">
        <div class="col-lg-12 col-md-12 col-xs-12 col-sm-12">
            <button onclick="telaInteiraLocal()">Tela Inteira Local</button>
            <button onclick="telaInteira()">Tela Inteira Remota</button>
        </div>        
        </div>
      
                                        <script>
                                            function telaInteira() {
                                                var elem = document.getElementById("remote-video");
                                                if (elem.requestFullscreen) {
                                                    elem.requestFullscreen();
                                                } else if (elem.mozRequestFullScreen) {
                                                    elem.mozRequestFullScreen();
                                                } else if (elem.webkitRequestFullscreen) {
                                                    elem.webkitRequestFullscreen();
                                                }
                                            }

                                            function telaInteiraLocal() {
                                                var elem = document.getElementById("local-video");
                                                if (elem.requestFullscreen) {
                                                    elem.requestFullscreen();
                                                } else if (elem.mozRequestFullScreen) {
                                                    elem.mozRequestFullScreen();
                                                } else if (elem.webkitRequestFullscreen) {
                                                    elem.webkitRequestFullscreen();
                                                }
                                            } 
                                        </script>
 
    <div class="row">
        <div class="col-lg-12 col-md-12  col-xs-12  col-sm-12">
            
            <video id="remote-video" class="videoRoboRemote" width="75%" autoplay>Remote</video>               
            
            <video id="local-video" class="videoRoboLocal" width="25%" autoplay>Local</video>
        </div>       

        <div  id="messages">
        </div>

    </div>
    <script src="../javascripts/peer.js"></script>
    <script src="../javascripts/main4.js"></script>
    
</body>
</html>
